<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shop"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".session3.MyCartActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity -->
        <!-- android:name=".session3.CartDetailsActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".session3.FavoritesCartActivity"
            android:exported="false" />
        <activity
            android:name=".session3.BottomNavigationActivity"
            android:exported="false" />
        <activity
            android:name=".Session2.VerificationActivity"
            android:exported="false" />
        <activity
            android:name=".Session2.ForgotPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".Session2.RegisterActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name=".Session2.SignInActivity" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name=".session1.OnBoardingActivity" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".session1.OnBoardingActivity"
            android:exported="false">

        </activity>
        <activity
            android:name=".session3.HomeActivity"
            android:exported="true">

        </activity>
    </application>

</manifest>